components:
  schemas:
    # [Body data] Sign out body data
    SignOutBodyData:
      type: object
      description: Sign out body data
      properties:
        refreshToken:
          type: string
      required:
        - refreshToken

    # [Response data] Sign out response
    SignOutResponseData:
      type: object
      description: Sign out response data
      properties:
